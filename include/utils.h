#ifndef UTILS_H
#define UTILS_H
#include <stdio.h>

// Utilites
int getUserInt(char *userPrompt, int *input);

// Math utilities
int calculateGCD(int a, int b);
int calculateLCM(int a, int b);

#endif
